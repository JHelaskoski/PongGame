# Pong Game

This project was added to GitHub as a single initial commit because it was originally developed locally.

A small hobby project made for fun using Python and Pygame.

## Requirements
-Python 3 installed
-Pygame installed with the command: pip install pygame
After installing Pygame, download or clone this repository, open the project folder, and run the game with the command: python PongGame.py.
